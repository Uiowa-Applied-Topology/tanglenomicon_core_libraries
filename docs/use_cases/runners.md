# Runners

A runner is a human/machine interface layer. This abstracts the routines in
lower layers for a user to interact with. This could be a CLI interface, python
bindings, a Mathematica wrapper, etc.

## Definitions

```{toctree}
:glob:
:maxdepth: 2
.cp_from_source/runner-*/use-case
```

```{raw} latex
    \newpage
```
