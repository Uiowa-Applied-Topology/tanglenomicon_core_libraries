# Use Case

The mutator interface defines the general form for a component used to mutate data in a program
flow. The mutator component does not allocate memory, it must be configured with sufficient buffer
space to successfully execute.

```{raw} latex
    \newpage
```
